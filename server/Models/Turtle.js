class Turtle {
    constructor(
         label,
         watIsPurpose,
         xPOS,
         yPOS,
         zPOS,
         fuelLvl
     ) 
     {
         this.label = label;
         this.watIsPurpose = watIsPurpose;
         this.xPOS = xPOS;
         this.yPOS = yPOS;
         this.zPOS = zPOS;
         this.fuelLvl = fuelLvl
     }
 }

 module.exports = Turtle;